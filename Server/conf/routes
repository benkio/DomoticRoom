# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /				    controllers.Application.redirect()
GET	/domoticRoom/status		    controllers.Application.status()
GET	/domoticRoom/analysis		    controllers.Analysis.analysis()
GET	/domoticRoom/newrange		    controllers.Application.newRange()
GET	/domoticRoom/ranges		    controllers.Application.ranges()
POST	/domoticRoom/submitNewRange	    controllers.Application.submitNewRange()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
